replicaCount: 1

image:
  tag: stable

ingress:
  host: pre.webapp.demo.local

config:
  environment: pre

resources:
  limits:
    cpu: "250m"
    memory: "256Mi"
  requests:
    cpu: "100m"
    memory: "128Mi"

autoscaling:
  enabled: false

namespace: web-pre
