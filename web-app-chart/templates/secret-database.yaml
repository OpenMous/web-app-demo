apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  postgres-db: {{ .Values.secrets.db.name }}
  postgres-user: {{ .Values.secrets.db.user }}
  postgres-password: {{ .Values.secrets.db.password }}
